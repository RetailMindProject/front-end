export interface ProfileFormData {
  firstName: string;
  lastName: string;
  address: string;
  email: string;
  phone: string;
}

export interface ProfileData {
  firstName: string;
  lastName: string;
  address: string;
  email: string;
  phone: string;
  role?: string;
}

